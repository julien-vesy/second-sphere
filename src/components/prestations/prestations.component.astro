---
import Droplet from '../../assets/droplet.svg'
import Refresh from '../../assets/refresh.svg'
import Scissor from '../../assets/scissor.svg'
import CircleCheck from '../../assets/circle-check.svg'
import CircleX from '../../assets/circle-x.svg'

const services = [
  {
    icon: Droplet,
    title: 'Formule 1',
    description: 'Nettoyage professionnel et polissage',
    price: 'À partir de 10€*',
    features: [
      'Nettoyage en profondeur',
      'Polisage holographique',
      'Produits spécialisés non-abrasifs',
      'Séchage contrôlé',
      { val: 'Pas de prise en charge des frais de transport**' },
    ],
  },
  {
    icon: Refresh,
    title: 'Formule 2',
    description:
      'Correction des plis et enfoncement léger pour retrouver une surface plane',
    price: 'À partir de 20€*',
    features: [
      'Le contenu de la Formule 1',
      "Correction d'un enfoncement léger",
      "Correction d'une pliure légère",
      'Résultats durables',
      { val: 'Pas de prise en charge des frais de transport**' },
    ],
  },
  {
    icon: Scissor,
    title: 'Formule 3',
    description: 'Restauration des coins et bordures abîmés',
    price: 'À partir de 35€*',
    features: [
      'Le contenu de la Formule 1',
      'Le contenu de la Formule 2',
      'Amélioration des zones endommagées',
      "Frais d'expedition retour offert**",
    ],
  },
]
---

<div class="py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h1 class="text-5xl mb-4 text-gray-900">Mes prestations</h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Des services professionnels adaptés à tous vos besoins de restauration
        de cartes Pokémon
      </p>
    </div>

    <div
      class="grid gap-8 mb-12 [grid-template-columns:repeat(auto-fit,minmax(350px,1fr))]"
    >
      {
        services.map((service) => (
          <div
            data-slot="card"
            class="bg-card text-card-foreground flex flex-col gap-6 rounded-xl border hover:shadow-xl transition-shadow"
          >
            <div
              class="flex flex-col gap-1.5 h-[140px] px-6 pt-6 "
            >
              <div class="mb-4">
                <service.icon class="w-10 h-10 text-blue-600" />
              </div>
              <h4 class="leading-none">{service.title}</h4>
              <p class="text-gray-500 text-muted-foreground">
                {service.description}
              </p>
            </div>
            <div data-slot="card-content" class="px-6 [&:last-child]:pb-6">
              <div class="mb-4">
                <span class="text-2xl text-blue-600">{service.price}</span>
              </div>
              <ul class="space-y-2">
                {service.features.map((feature) => (
                  <li class="flex items-start">
                    {typeof feature === 'string' && (
                      <CircleCheck class="w-5 h-5 text-green-500 mr-2 flex-shrink-0 mt-0.5" />
                    )}
                    {typeof feature !== 'string' && (
                      <CircleX class="w-5 h-5 text-red-500 mr-2 flex-shrink-0 mt-0.5" />
                    )}
                    {typeof feature === 'string' && (
                      <span class="text-gray-700">{feature}</span>
                    )}
                    {typeof feature !== 'string' && (
                      <span class="text-gray-700">{feature.val}</span>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          </div>
        ))
      }
    </div>
    <div class="text-sm mb-4">
      <i>
        *Les tarifs restent indicatif et soumis à l'évaluation d'un devis.
      </i>
      <br />
      <i>
        **Fix My Card ne prend pas en charge les frais de transport ou encore
        l'assurance de transport.
      </i>
      <a class="underline" href="/">Voir cgv.</a>
    </div>
    <div
      class="bg-gradient-to-br from-blue-50 to-indigo-100 rounded-lg p-8 mb-12"
    >
      <h2 class="text-3xl mb-8 text-center text-gray-900">
        Le processus de restauration
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <div class="text-center">
          <div
            class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl"
          >
            1
          </div>
          <h3 class="text-xl mb-2">Diagnostic</h3>
          <p class="text-gray-700">
            Analyse détaillée de l'état de votre carte
          </p>
        </div>
        <div class="text-center">
          <div
            class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl"
          >
            2
          </div>
          <h3 class="text-xl mb-2">Devis</h3>
          <p class="text-gray-700">Proposition personnalisée et transparente</p>
        </div>
        <div class="text-center">
          <div
            class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl"
          >
            3
          </div>
          <h3 class="text-xl mb-2">Restauration</h3>
          <p class="text-gray-700">
            Intervention experte avec techniques professionnelles
          </p>
        </div>
        <div class="text-center">
          <div
            class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl"
          >
            4
          </div>
          <h3 class="text-xl mb-2">Retour</h3>
          <p class="text-gray-700">
            Carte restaurée et protégée, livrée avec soin
          </p>
        </div>
      </div>
    </div>

    <div class="text-center">
      <h2 class="text-3xl mb-4 text-gray-900">Intéressé par mes services ?</h2>
      <p class="text-xl text-gray-600 mb-6">
        Demandez un devis gratuit et personnalisé dès maintenant
      </p>
      <button
        onclick={`location.href='devis'`}
        class="bg-primary text-primary-foreground hover:bg-primary/90 inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive bg-blue-600 hover:bg-blue-700"
      >
        Demander un devis gratuit
      </button>
    </div>
  </div>
</div>
